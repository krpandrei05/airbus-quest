<!--  ══════════════════════════════════════════════════════════
         MAIN SHELL — The "sandwich" container
         This layout wraps ALL screens that have the Bottom Nav + Drawer.
         Structure:
           DrawerLayout (root)
             ├── CoordinatorLayout (main content)
             │     ├── Toolbar (dark, top)
             │     ├── FrameLayout (content area — fragments go here)
             │     └── BottomNavigationView (dark, bottom)
             └── NavigationView (drawer, slides from left)

         Used by: MainActivity (which hosts fragments for each tab)
         NOT used by: LoginActivity, GameOverActivity (standalone screens)
         ══════════════════════════════════════════════════════════  -->
<androidx.drawerlayout.widget.DrawerLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawerLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true">

    <!--  ═══ MAIN CONTENT AREA ═══  -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:background="@color/background">

        <!--  Dark toolbar  -->
        <com.google.android.material.appbar.MaterialToolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="@dimen/toolbar_height"
            android:background="@color/primary"
            android:elevation="4dp"
            app:titleTextColor="@color/text_on_dark"
            app:navigationIconTint="@color/text_on_dark"
            app:title="@string/app_name"/>

        <!--  Fragment container: each bottom nav tab swaps a fragment here  -->
        <FrameLayout
            android:id="@+id/fragmentContainer"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"/>
        
        <!--  Green accent line above bottom nav  -->
        <View
            android:layout_width="match_parent"
            android:layout_height="@dimen/bottom_nav_top_line"
            android:background="@color/bottom_nav_top_line"/>

        <!--  Dark bottom navigation bar  -->
        <com.google.android.material.bottomnavigation.BottomNavigationView
            android:id="@+id/bottomNav"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@color/bottom_nav_bg"
            app:itemIconTint="@color/bottom_nav_item_colors"
            app:itemTextColor="@color/bottom_nav_item_colors"
            app:labelVisibilityMode="labeled"
            app:menu="@menu/bottom_nav_menu"/>
    </LinearLayout>

    <!--  ═══ NAVIGATION DRAWER ═══  -->
    <com.google.android.material.navigation.NavigationView
        android:id="@+id/navView" android:layout_width="280dp"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:fitsSystemWindows="true"
        app:headerLayout="@layout/nav_header"
        app:menu="@menu/drawer_menu"
        app:itemTextColor="@color/text_primary"
        app:itemIconTint="@color/text_secondary"/>
</androidx.drawerlayout.widget.DrawerLayout>